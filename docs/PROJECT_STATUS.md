# Pika 项目状态（最新）

## 现状

**就医服务 POC v0.1 验证通过。** `hospital` 贯通、多图上传、上传两阶段（草稿→人工修正→提交）均已实现并落地。

模拟器与真机都跑通了完整场景（登录→多图上传→草稿编辑→提交→历史/详情/趋势）。后端部署在绿联 NAS，`http://192.168.1.200:8000/health` 通；Azure 视觉解析实测可用，解析出的文字与数字经人工核对正确。

**当前焦点：打牢工程底座（v0.2 基建期）。** 详见 `IMPLEMENTATION_PLAN.md`。

## 下一步（按优先级）

1. **C1 引入 Alembic 迁移**（基石）：摆脱「改字段就删库重建」，解锁后续所有 schema 变更。
2. **C2 自动化测试骨架**：pytest 覆盖 vision 后处理（数值/参考范围/abnormal_flag）+ draft/commit 流程，Azure 调用用 mock。
3. **B1 删除报告接口**：补 `DELETE`，用于清理测试期脏数据/重复记录（改动小，不动 models）。
4. **B2 重复上传去重**（依赖 C1）：加图片内容 hash，避免同单子重复落库与趋势重复点。
5. **C3 失败可观测**：上传失败的日志/重试入口。
6. （后续 v0.3+）多图子表化、草稿持久化、成员管理与共享、公网 HTTPS。

## 关键约定（易踩坑）

- DB 当前用 `create_all`，不给旧表加新列；改 models 字段需删 NAS 上 `pika.db` 重建（清数据，先征得同意）。**C1 完成后由 Alembic 取代此约定。**
- 真实凭据只在 NAS 的 `.env`，不入 git。
- 架构/命令见 `CLAUDE.md`；范围与执行顺序见 `IMPLEMENTATION_PLAN.md`。
