# Pika 项目状态（最新）

## 现状

**就医服务 POC v0.1**：`hospital` 贯通、多图上传、上传两阶段（草稿→人工修正→提交）均已实现。

后端已部署到绿联 NAS，`http://192.168.1.100:8000/health` 通；Azure `gpt-5.4-mini` 视觉解析实测可用。**当前焦点：小程序真机联调。**

## 下一步

1. **真机联调（当前）**：微信开发者工具勾「不校验合法域名」连 NAS IP，跑通 登录→多图上传→草稿编辑→提交→历史/详情/趋势。
2. **解析质量调优**：真实单据评估，必要时调 `prompts.py` 与后处理。
3. **稳定性回归**：连续上传验证；确认失败兜底（原图不丢、status=failed）。
4. （后续）公网 HTTPS、成员管理、更多检查类型与模块。

## 关键约定（易踩坑）

- DB 用 `create_all`，不给旧表加新列。改了 models 字段需删 NAS 上 `pika.db` 重建（清数据，先征得同意）。
- 真实凭据只在 NAS 的 `.env`，不入 git。
- 架构/命令见 `CLAUDE.md`；范围与执行顺序见 `IMPLEMENTATION_PLAN.md`。
