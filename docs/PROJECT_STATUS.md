# Pika 项目状态（最新）

## 当前状态

- 主线阶段：体验版稳定性回归 + 成员管理放量前验收。
- 已落地：成员管理管理员闭环（新增/编辑/角色/移除恢复）、小程序内二维码扫码加入、登录与头像稳定性修复、扫码入口统一到“我”页。
- 部署状态：后端已发布到 NAS，Alembic 版本 `d5a9e1c7b2f4`。
- 构建优化：Dockerfile 默认快速安装依赖（`PIP_NO_CACHE=0`），可按需切换无缓存构建。

## Backlog（唯一入口）

### P0（当前必须先做）
1. 真机验收小程序内扫码加入闭环（生成二维码 → “我”页扫一扫 → 确认加入）。
2. 回归登录首轮成功率（跨网络、多次）。
3. 回归头像更换后回页不短暂丢失。
4. 定位并修复开发者工具中成员管理页偶发空白（体验版正常、模拟器异常）。

### P1（P0 完成后）
1. 多账号放量前权限回归（至少 2 个体验成员）。
2. 普通用户权限边界验证（不能删改他人报告/角色）。

### P2（后续功能）
1. 趋势图升级。
2. AI 助手能力迭代。
3. 更多报告类型适配。

## 关键约定（维持）

- `PROJECT_STATUS.md` 是 backlog 唯一入口；新会话优先读取本文件。
- DB 由 Alembic 管理；改 models 后 `alembic revision --autogenerate`。
- 改 backend 代码须 **rebuild**（重启不够）；改挂载须同步 NAS `docker-compose.yaml`。
- 流程：先 release→模拟器/真机验证→再 commit；push 前检查敏感公网标识不入仓库。
- 真实凭据只在 NAS `.env`，不入 git。
- 架构与运行约束见 `CLAUDE.md`。
