Page({
  goFocusMetrics() {
    wx.navigateTo({ url: '/pages/medical/focus-metrics/focus-metrics' });
  },

  goReportCategories() {
    wx.navigateTo({ url: '/pages/medical/report-categories/report-categories' });
  },

  goPermissions() {
    wx.navigateTo({ url: '/pages/medical/permissions/permissions' });
  },
});
